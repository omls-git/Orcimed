# Docs: https://github.com/Azure/webapps-deploy

name: Azure Web App CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Create .env file
        working-directory: Orcimed
        run: |
          echo "REACT_APP_API_BASE_URL=${{ secrets.REACT_APP_API_BASE_URL }}" >> .env
          echo "AZURE_MYSQL_DATABASE=${{ secrets.AZURE_MYSQL_DATABASE }}" >> .env
          echo "AZURE_MYSQL_HOST=${{ secrets.AZURE_MYSQL_HOST }}" >> .env
          echo "AZURE_MYSQL_PASSWORD=${{ secrets.AZURE_MYSQL_PASSWORD }}" >> .env
          echo "AZURE_MYSQL_PORT=${{ secrets.AZURE_MYSQL_PORT }}" >> .env
          echo "AZURE_MYSQL_SSL=${{ secrets.AZURE_MYSQL_SSL }}" >> .env
          echo "AZURE_MYSQL_USER=${{ secrets.AZURE_MYSQL_USER }}" >> .env

      - name: Install dependencies and build
        working-directory: Orcimed
        run: |
          npm install
          npm run build --if-present
          npm run test --if-present

      - name: Zip artifact for deployment
        working-directory: Orcimed
        run: zip -r release.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: Orcimed/release.zip

  deploy:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action == 'closed')
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Unzip artifact
        run: unzip release.zip

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'omls-website' # Replace with your actual App Service name
          slot-name: 'Production'  # Optional: use if deploying to a specific slot
          package: .
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_924283B181FD4275AABED26CE5856552 }}
